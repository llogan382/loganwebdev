version: 1.1.26
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - yarn add sharp
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
  appRoot: dynamic-routing
